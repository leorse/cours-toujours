# Copier ce fichier en .env.local et renseigner l'IP du PC sur ton réseau WiFi
# Pour trouver l'IP : ipconfig (Windows) ou ip addr (Linux)
#
# Ce fichier est lu par capacitor.config.ts lors du npx cap sync
# Il ne sert pas à Vite (pas de préfixe VITE_)

SERVER_URL=http://192.168.1.x:8080
